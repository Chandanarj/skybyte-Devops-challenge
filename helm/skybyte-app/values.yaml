replicaCount: 1

image:
  repository: skybyte/app
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

# API token used by the service. Rotate quarterly.
apiToken: "my-secret-value"

namespace: devops-challenge
